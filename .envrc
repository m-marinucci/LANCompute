# Activate virtualenv if present
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi

# Load environment variables from .env if present (no direnv stdlib required)
if [ -f .env ]; then
  set -a
  . ./.env
  set +a
fi
