# Copy to .env and adjust for your environment

# Base URL for the LM Studio/Ollama-compatible API
# Example for local dev via SSH tunnel or local service
LM_STUDIO_BASE_URL=http://127.0.0.1:1234

# Optional: prefer a specific test model id
# LM_STUDIO_TEST_MODEL=mistral:latest

# Optional: custom test prompt for the integration test
# LM_STUDIO_TEST_PROMPT=Reply with a short acknowledgement.
